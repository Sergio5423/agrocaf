plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.agrocaf"
    compileSdkVersion 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = "11"  // 🔹 Cambiado a Java 11
    }

    defaultConfig {
        applicationId = "com.example.agrocaf"
        minSdkVersion 23
        targetSdkVersion 34  // 🔹 Arreglado
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
    release {
        minifyEnabled true  // 🔹 Habilitar reducción de código
        shrinkResources true  // 🔹 Habilitar eliminación de recursos no usados
        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
    }
}

}

flutter {
    source = "../.."
}
